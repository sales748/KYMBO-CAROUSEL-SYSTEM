node_modules/
.DS_Store
*.log

# Rendered screenshot-source HTML (regenerated by `npm run build`)
profiles/*/build/carousels/
# Delivery zips are rebuilt on demand from export/<batch>/<carousel>/ — the
# JPGs and @2x PNGs are the tracked artifacts, the zips are just packaging.
# (anchored pattern 'export/*.zip' never matched profiles/kymbo/export/)
**/export/*.zip
